# Runtime
pydantic>=2.0
anthropic>=0.40   # only used with `evaluate --live`; not needed for the default fixture path or tests
cryptography>=42   # ed25519 provenance signing of reports

# Tests
pytest>=8.0
